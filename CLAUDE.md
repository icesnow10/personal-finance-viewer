# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Maintaining this file

Every new feature, architectural change, or new local file must update this `CLAUDE.md` to reflect the change. Keep it accurate and in sync with the codebase.

## Plugin dependency

This viewer works in tandem with the **[open-personal-finance](https://github.com/icesnow10/personal-finance)** Claude Code plugin, which handles fetching bank transactions (via Pluggy API), classifying expenses, recognizing income, and generating the budget JSON files this app consumes.

Before running any financial analysis (`/compile`, `/heartbeat`, `/fetch`, etc.), always update the plugin to get the latest classification rules and skill improvements:

```
/plugin marketplace add icesnow10/personal-finance
/reload-plugins
```

## Commands

```bash
npm run dev       # start dev server at localhost:3000
npm run build     # production build
npm run lint      # ESLint via next lint
```

No test suite is configured.

## Architecture

**Next.js Pages Router** app. All pages are in `src/pages/`, API routes in `src/pages/api/`.

### Data flow

1. Budget JSON files live in `resources/{YYYY-MM}/expenses/result/budget_{MM}_{YYYY}.json` (gitignored, generated by the [personal-finance](https://github.com/icesnow10/personal-finance) plugin).
2. `GET /api/budgets` scans the resources directory, reads all valid budget JSONs, and returns them sorted by month. The resources path defaults to `{cwd}/resources` but can be overridden via `.config.json` (also gitignored): `{ "resourcesPath": "/absolute/path" }`.
3. `BudgetContext` (`src/context/BudgetContext.tsx`) fetches from `/api/budgets` on mount, exposes `allMonths: BudgetData[]`, `data: BudgetData` (most recent month), and `flatTransactions: FlatTransaction[]`.
4. Pages consume data via `useBudget()` hook.

### Key types (`src/lib/types.ts`)

`BudgetData` is the central type — every budget JSON must conform to it. Key fields:
- `income.items[]` — income transactions (salary, dividends, reimbursements)
- `expenses.by_category` — `Record<CategoryName, CategoryData>` → subcategories → transactions
- `expenses.unclassified[]` — uncategorized transactions
- `summary` — pre-computed totals (income, expenses, net, investment, investment_pct)
- `budget_buckets` — exactly 3 keys: `custos_fixos`, `conforto`, `liberdade_financeira`
- `skipped[]` — internal transfers excluded from totals
- `intentional_rdb_investments` — large lump-sum investments tracked separately, NOT in expense totals
- `partial: true` + `data_through` — marks months with incomplete data
- `status` (`"posted" | "pending"`) — per-row flag from the plugin. Credit charges are `pending` while the bill is open, `posted` once it closes. Carried through `normalizeBudget` (baseTx allow-list), `flattenTransactions`, and `FlatTransaction`. Missing status defaults to `posted`. Surfaced as a **Status** column + inline "Pendente" tag in the transactions table, and as a top-right warning badge on the overview **Resultado Parcial** card when any pending rows remain.

### Computation layer (`src/lib/computations.ts`)

Pure functions over `BudgetData` used by pages:
- `getSpendingPace` — daily avg, projected total, comparison vs previous month
- `getBucketProgress` — recalculates bucket amounts from `expenses.by_category` (source of truth is the category data, not the pre-computed bucket fields in the JSON)
- `getDailySpendingCurve` — cumulative spending by day for chart rendering
- `getRecurringTransactions` — finds transactions appearing in ≥2 months

### Contexts

| Context | Purpose |
|---|---|
| `BudgetContext` | Budget data, loading state, refresh trigger |
| `ThemeContext` | Ant Design light/dark mode toggle |
| `RedactContext` | Hides monetary values (eye toggle in header) |

### Responsive layout

`useIsMobile` (`src/hooks/useIsMobile.ts`) is a `matchMedia(max-width: 767px)` hook used across pages. `AppShell` switches to a mobile layout below 768px: the left `Sider` is hidden and a fixed bottom navigation (`/overview`, `/transactions`, `/investments`, `/settings`) replaces it. `Content` adds bottom padding equal to the nav height (64px) plus `env(safe-area-inset-bottom)`. Pages collapse two-column grids to a single column at the same breakpoint and `VisorCard` reduces internal padding.

### Pages

| Route | File | What it renders |
|---|---|---|
| `/overview` | `src/pages/overview/index.tsx` | Spending pace, budget buckets, category tree, installments + monthly composition (à vista / parcela nova / parcela antiga), top-10 categories trend, monthly income bars, wealth evolution (cumulative monthly net). **Principais Categorias** rows expand into a per-category installment tree (`CategoryInstallmentTree`) breaking spend into parcela antiga / nova / à vista / provisionado, each drillable to its transactions. The **Comparar** modal (`CategoryComparisonModal`) supports multi-selecting category rows with a sticky totalizer footer summing the selection per month + Média + Total. |
| `/transactions-v2` | `src/pages/transactions-v2/index.tsx` | Searchable/filterable flat transaction table |
| `/income` | `src/pages/income/index.tsx` | Income breakdown by month and source |
| `/settings` | `src/pages/settings/index.tsx` | Resources path configuration (writes `.config.json`) |

### Scheduled prompts (top-bar modal)

Terminal icon in the header opens `PromptShortcutModal` (`src/components/PromptShortcutModal.tsx`), which lists cron-scheduled prompt runs via `node-cron` (`src/lib/scheduler.ts`). Schedules persist to `.pfv-schedules.json` (gitignored) and are re-registered on every server start. Timezone: America/Sao_Paulo.

CRUD via `/api/schedules`: `GET` (list), `POST` (create), `PUT` (update), `DELETE` (remove), `PATCH` (run now). Each tick calls `runShortcut(prompt, { autoClose: true })` in `src/lib/runShortcut.ts`, which writes a temp `.ps1` that `Set-Location`s to the configured `resourcesPath` and spawns `cmd /c start "" powershell -File <script>` to run `claude "<prompt>"`. Cron only fires while the Next.js server is running.

No session tracking or status reporting — agent lifecycle is handled elsewhere. Windows-only (returns error on other platforms).

### Budget bucket definitions

The three buckets map categories as follows:
- **Custos Fixos (30%)**: Housing, Health, Insurance, Groceries, Transportation
- **Conforto (25%)**: Wellness, Subscriptions, Personal Care, Services, Food/Dining, Recreation, Shopping, Travel, Family Support, Education, and any unlisted category (catch-all)
- **Liberdade Financeira (45%)**: `Investment (Troco Turbo)` + Net

`Investment (Troco Turbo)` must be the exact key in `expenses.by_category` for it to appear correctly in the liberdade_financeira bucket.

## Local data files (gitignored)

| File | Purpose |
|---|---|
| `resources/` | All financial data |
| `.env.local` | Pluggy credentials (`PLUGGY_CLIENT_ID`, `PLUGGY_CLIENT_SECRET`, `PLUGGY_ITEM_IDS`) |
| `.config.json` | Resources path override for the viewer |
| `resources/pluggy_items.json` | Maps Pluggy item UUIDs → holder names + account metadata |
| `resources/expenses_memory.md` | Merchant→category mappings, budget bucket definitions, RDB threshold |
| `resources/income_inputs.md` | Salary definitions (amounts, date windows, holders) |
| `generate_budgets.js` | Temporary compile script generated by `/compile` — contains transaction data, never commit |
